# 2026-07-24 开发记录：编辑、重新上架、测试数据清理

## 本次完成

- 内容管理列表增加“预览”入口。
- 已下线内容增加“重新上架”按钮，可恢复为已发布状态。
- 内容管理增加“清理测试数据”按钮，仅清理标题以“浏览器验证”开头的测试文章和测试招聘。
- 内容编辑页重写中文 UI，支持标题、正文、招聘公司、状态保存。
- 内容预览页重写中文 UI，展示标题、状态、公司、日期和正文。
- 操作日志增加“清理浏览器测试数据”的中文显示。
- 常驻启动脚本增加 `.next/static` 到 standalone 的同步，修复生产构建后浏览器加载旧 chunk 的问题。

## 验证结果

- `npm run typecheck`：通过。
- `npm run build`：通过。
- `GET /api/health`：返回 `database: connected`。
- `YanglinLocalSite`：任务状态为 `Running`。
- 浏览器验证后台登录成功。
- 浏览器创建测试文章成功。
- 浏览器编辑测试文章成功，页面提示“保存成功”。
- 浏览器打开预览页成功，显示编辑后的标题和正文。
- 浏览器下线测试文章成功，状态变为“已下线”。
- 浏览器重新上架测试文章成功，状态变为“已发布”。
- 浏览器清理测试数据成功，提示删除文章 2 条、招聘 1 条。
- 浏览器访问前台文章和招聘搜索“浏览器验证”，均显示 0 条，确认测试数据已清理。
- 浏览器访问操作日志，清理动作显示为“清理浏览器测试数据”。

## 剩余事项

- 分类信息模块最小闭环尚未开始。
- 旧数据迁移样本尚未开始。
- 后台仍有历史测试数据标题为 `1`，不属于本次“浏览器验证”清理范围。
